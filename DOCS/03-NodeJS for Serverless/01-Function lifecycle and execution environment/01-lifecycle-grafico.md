
# 🟦 **Schema Grafico Testuale – Serverless Function Lifecycle (Node.js)**

**Schema grafico testuale**, chiaro e leggibile, che rappresenta il **lifecycle di una funzione serverless Node.js**.


```
                   ┌─────────────────────────────┐
                   │        DEPLOY / UPDATE      │
                   └──────────────┬──────────────┘
                                  │
                                  ▼
                    ┌─────────────────────────┐
                    │     COLD START (INIT)   │
                    ├─────────────────────────┤
                    │ - Creazione ambiente    │
                    │   runtime isolato       │
                    │ - Caricamento moduli    │
                    │   Node.js               │
                    │ - Esecuzione del codice │
                    │   globale               │
                    │ - Connessioni DB, SDK   │
                    └──────────────┬──────────┘
                                   │
                                   ▼
                     ┌────────────────────────┐
                     │      INVOCATION        │
                     ├────────────────────────┤
                     │ - Chiamata handler     │
                     │ - Logica applicativa   │
                     │ - Accesso risorse      │
                     │   già inizializzate    │
                     │ - Risposta utente      │
                     └──────────────┬─────────┘
                                    │
                        ┌───────────┴─────────────┐
                        │                         │
                        ▼                         ▼
           ┌──────────────────────┐   ┌────────────────────────────┐
           │   FREEZE / IDLE      │   │   NUOVA INVOCATION ARRIVA  │
           ├──────────────────────┤   └────────────────────────────┘
           │ - Ambiente congelato │                 │
           │ - CPU sospesa        │                 ▼
           │ - Memoria conservata │     ┌───────────────────────────┐
           │   (cache, conn.)     │     │   WARM START (THAWING)    │
           └──────────┬───────────┘     ├───────────────────────────┤
                      │                 │ - Ambiente riattivato     │
                      │                 │ - Stato RAM preservato    │
                      │                 │ - Nessuna re-init globale │
                      │                 └──────────┬────────────────┘
                      │                            │
                      └────────────────────────────|
                                                   |
                                                   ▼
                                  ┌────────────────────────┐
                                  │ INVOCATION   (handler) │
                                  └────────────────────────┘
                         
                                     
```

---

# 🟨 **Legenda**

* **Cold Start** → ambiente nuovo, init completo
* **Warm Start** → riutilizza ambiente freezato
* **Freeze** → runtime sospeso ma memoria viva
* **Shutdown** → ambiente eliminato definitivamente

